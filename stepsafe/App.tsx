import React from 'react';
// Adjust this import path if your App.tsx is inside src/ instead of the root
import ProfileScreen from './src/screens/ProfileScreen'; 

export default function App() {
  return <ProfileScreen />;
}